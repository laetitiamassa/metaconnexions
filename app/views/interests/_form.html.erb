<%= simple_form_for(@interest) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :interest_name_id %>
    <%= f.input :user_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
