<%= simple_form_for(@interest) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :interest_name_id %>
    <%= f.input :user_id %>
    <%= f.select :how, options_for_select(["De coeur","D'actions"],:disabled => ["De quelle manière ?"]) %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
